[package]
name = "manual_expense"
type = "bin"
authors = [""]
# Pinned to the nargo/Noir release this circuit was written and reviewed
# against -- see ../README.md for exactly which compiler build this maps
# to and why (a known nargo 1.0.0-beta.15 x bbup incompatibility is why
# this is NOT pinned to beta.15; see README "Toolchain versions").
compiler_version = ">=1.0.0-beta.20"

[dependencies]
poseidon = { tag = "v0.1.1", git = "https://github.com/noir-lang/poseidon" }
